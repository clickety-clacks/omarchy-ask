# Omarchy Ask 0.7.1

- Restore application search on Omarchy 4.0.4, whose scoped overlay-plugin API
  no longer supplies Ask with the shared app library.
- Launch matching desktop entries through the standard desktop launcher when
  that API is unavailable.
- Show application matches before menu, file, repository, and window results.
